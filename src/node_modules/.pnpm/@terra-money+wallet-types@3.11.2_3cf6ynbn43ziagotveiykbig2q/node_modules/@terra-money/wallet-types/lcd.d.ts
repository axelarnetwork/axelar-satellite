import { LCDClient, LCDClientConfig } from '@terra-money/terra.js';
import { NetworkInfo } from './types';
declare type Config = Omit<LCDClientConfig, 'URL' | 'chainID'>;
export declare type WalletLCDClientConfig = Config | ((network: NetworkInfo) => Config);
interface Params {
    lcdClientConfig?: WalletLCDClientConfig;
    network: NetworkInfo;
}
export declare function createLCDClient({ lcdClientConfig, network, }: Params): LCDClient;
export {};
